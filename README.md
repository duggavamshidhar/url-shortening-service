# URL Shortening Service

A RESTful web service built with Java, Spring Boot, and MVC to shorten URLs, manage users securely, track analytics, and
handle expiration. It uses PostgreSQL, Redis, JUnit for testing, Maven for builds, and Docker for containerized
deployment.
